# M7.8 — Android Packaging Preparation

**Status:** ACTIVE — PACKAGING PREP  
**Project:** @Orlumi Studio → @Petal Nook  
**Date:** 2026-09-13

## Objective

Move the current playable Godot build toward an installable Android package without changing the already locked gameplay, MainRoom layout, HUD information architecture or furniture progression.

## Current baseline

- Godot 4.3 production project
- Android-first portrait target
- MainRoom Layout A locked
- GDevelop-derived top-left status / top-right controls retained
- Shop uses the 2×2 card structure
- Little Pot relocates onto Small Table when the table is owned
- Lumie roaming excludes owned furniture regions
- save/reset/offline/photo-mode gameplay remains integrated
- latest shop integration smoke test passed before this milestone

## Android export preset

`export_presets.cfg` now defines an `Android` preset with:

- package id: `com.orlumi.petalnook`
- app name: `Orlumi: Petal Nook`
- portrait project viewport inherited from `project.godot`
- arm64-v8a target
- APK development export path: `build/android/Orlumi-Petal-Nook.apk`
- version name `0.8.0`
- version code `1`

This is packaging configuration only. A successful Android APK/device result must not be claimed until an Android SDK/export-template environment actually performs the export and the APK is installed on a device/emulator.

## Remaining production blockers before owner delivery

1. Replace developer visual fallbacks with production-approved PNGs.
2. Run integrated MainRoom visual QA at representative furniture levels.
3. Confirm Lv4/Lv5 Small Table keeps the Little Pot centre slot clear.
4. Confirm Curtain B alignment against the real window art.
5. Confirm Wooden Rack progression remains relevant to Petal Nook and contains no unrelated props.
6. Confirm Lumie remains recognisable and readable at final in-room scale.
7. Run asset-contract validation across every final asset.
8. Export Android APK in a configured Android build environment.
9. Install and test on Android hardware/emulator.
10. Re-run save/reset/offline/photo/Lumie/furniture progression checks on the packaged build.

## Delivery gate

Do not stop for owner review merely because a production task advances. Raise a new owner gate only if an integrated preview exposes a genuine aesthetic choice or a contradiction with already approved direction.
